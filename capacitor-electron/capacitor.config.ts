import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.devioarts.examples.electron',
  appName: 'capacitor-electron',
  webDir: 'dist'
};

export default config;
