import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.devioarts.exampleapp',
  appName: 'capacitor-exampleapp',
  webDir: 'dist'
};

export default config;
